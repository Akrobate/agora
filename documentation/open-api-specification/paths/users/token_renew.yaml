post:
  summary: Get types
  tags:
    - user
  responses:
    200:
      description: Return new token
      content:
        application/json:
          schema:
            type: object
            properties:
              token:
                type: string
    401:
      description: Not authorized
  security:
    - bearer: []
