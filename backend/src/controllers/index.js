'use strict';

const {
    CampaignController,
} = require('./CampaignController');

const {
    UserController,
} = require('./UserController');

module.exports = {
    CampaignController,
    UserController,
};
