'use strict';

const {
    BordaAlgorithm,
} = require('./BordaAlgorithm');

const {
    RelativeMajorityAlgorithm,
} = require('./RelativeMajorityAlgorithm');

module.exports = {
    BordaAlgorithm,
    RelativeMajorityAlgorithm,
};
