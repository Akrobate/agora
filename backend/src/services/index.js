'use strict';

const {
    CampaignService,
} = require('./CampaignService');

const {
    UserService,
} = require('./UserService');

const {
    CampaignUserService,
} = require('./CampaignUserService');

const {
    PropositionService,
} = require('./PropositionService');

module.exports = {
    CampaignService,
    CampaignUserService,
    UserService,
    PropositionService,
};
